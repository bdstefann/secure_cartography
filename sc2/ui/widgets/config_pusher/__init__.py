"""PyQt6 widgets for the Config Push tool.

The widgets compose into ConfigPusherDialog (opened from the MainWindow
header). All business logic lives in sc2.scng.tools.config_pusher and
sc2.scng.tools.history_db — this package is Qt glue only.
"""
